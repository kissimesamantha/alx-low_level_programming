#include "main.h"

/**
 * _islower - functions that checks for lowercase characters
 *
 * Return:1 if c islowercase 0 otherwise
 */
int _islower(int c)
{
	if (c = 'a' && c <= 'z')
		return (1);

	if else (c = 'A' && c <= 'Z')
		return (0);

}
